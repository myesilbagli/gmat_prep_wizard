/** Must match RevenueCat dashboard entitlement identifier. */
export const LEXICON_PRO_ENTITLEMENT = 'lexicon_pro'

/** Default offering id in RevenueCat (create offering "default" with monthly + annual packages). */
export const REVENUECAT_DEFAULT_OFFERING = 'default'

/**
 * App Store Connect product IDs — register these exactly in ASC + RevenueCat.
 * Change only if your store product ids differ.
 */
export const STORE_PRODUCT_MONTHLY = 'lexicon_pro_monthly'
export const STORE_PRODUCT_YEARLY = 'lexicon_pro_yearly'
