# Copy to `.env` at repo root and fill in. `.env` is gitignored.
OPENAI_API_KEY=

# Optional
# OPENAI_GENERATE_MODEL=gpt-4.1
# RC Stage 1 only — /generateRcPassage on Firebase Functions. Defaults to gpt-5 if unset.
# OPENAI_RC_PASSAGE_MODEL=gpt-5
# GLOSSARY_STACKS_PATH=
